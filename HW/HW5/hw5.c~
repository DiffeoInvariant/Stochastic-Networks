static char help[] = "Homework 5 Problem 1 code, solves the Kuramoto model.\n\n";

#include <petscts.h>

struct _kuramoto_data {
  PetscInt    N;
  Vec         omega;
  PetscScalar K;
};

typedef struct _kuramoto_data *User;

/* F is the output parameter */
static PetscErrorCode RHSFunction(TS ts, PetscReal t, Vec omega, Vec F, void* ctx)
{
  PetscErrorCode ierr;
  PetsFunctionBeginUser;


  PetscFunctionReturn(0);
}
